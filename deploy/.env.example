# ============================================================
# MeetSpace Manager — Environment Configuration
# ============================================================
# Copy this file to .env and fill in the values before deploying.
#   cp .env.example .env
# ============================================================

# ── PostgreSQL Database ──────────────────────────────────────
POSTGRES_DB=meetspace
POSTGRES_USER=meetspace
POSTGRES_PASSWORD=CHANGE_ME_to_a_strong_password

# ── Application ──────────────────────────────────────────────
# Secret used to sign session cookies (generate a random string)
# Example: openssl rand -hex 32
SESSION_SECRET=CHANGE_ME_generate_a_random_secret

# ── Admin Account (created on first run) ─────────────────────
ADMIN_PASSWORD=CHANGE_ME_strong_admin_password
ADMIN_EMAIL=admin@yourcompany.com

# ── Email (SendGrid) — Optional ──────────────────────────────
# Required for booking confirmations, registration alerts, etc.
# Get your API key at https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@yourcompany.com

# ── Microsoft 365 Integration — Optional ─────────────────────
# Required for Teams room calendar sync, auto-creating calendar events,
# and webhook-based real-time calendar sync.
# Create an App Registration in Azure AD:
#   https://portal.azure.com > Azure Active Directory > App registrations
# Required API permissions (Application): Calendars.ReadWrite, Place.Read.All
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=

# ── Webhook Configuration — Optional ─────────────────────────
# Public HTTPS URL where Microsoft Graph can send webhook notifications.
# Must be accessible from the internet. Example: https://meetspace.yourcompany.com
# Required only if using real-time calendar sync via Microsoft Graph webhooks.
WEBHOOK_BASE_URL=https://meetspace.yourcompany.com
